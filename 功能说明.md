# 永劫无间藏宝阁查询插件功能说明

## 插件用途

`astrbot_plugin_yjcbg` 是一个可导入 AstrBot 的永劫无间藏宝阁查询插件，用于在聊天中查询藏宝阁公开商品/账号数据。

藏宝阁地址：

```text
https://yjwujian.cbg.163.com/
```

## 支持功能

### 1. 藏宝阁搜索

按关键词查询藏宝阁商品/账号列表。支持英雄名、皮肤名、外观名、宝箱名、礼包名等模糊搜索，**覆盖全部有效分类**：英雄皮肤（`kindid=3`）、武器皮肤（`kindid=4`）、宝箱（`kindid=5`）、礼包（`kindid=6`）。

命令：

```text
/永劫藏宝阁 搜索 关键词
```

别名：

```text
/藏宝阁 搜索 关键词
/永劫藏宝阁 查 关键词
/永劫cbg 搜索 关键词
```

示例：

```text
/永劫藏宝阁 搜索 胡桃
/永劫藏宝阁 搜索 土御门胡桃
/永劫藏宝阁 搜索 殷紫萍
/永劫藏宝阁 搜索 风之絮语
```

返回内容包括：

- 商品名称
- 变体（颜色/图纹/衣裁等）
- 品质/英雄
- 价格
- 服务器
- 区服/平台
- 商品编号（Y/S 等字母开头）
- 星格（`variation_id`，用 `-` 分隔的多段星格数值）
- 收藏数
- 卖家
- 洗练次数
- 状态（公示期/在售期）
- 公示期截至时间
- 在售期开始时间
- 抽签状态
- 藏宝阁详情链接

### 2. 翻页查询

搜索结果默认每页显示 5 条（可通过配置项 `max_results` 调整）。当结果超过一页时，会显示总页数和下一页提示。

示例：

```text
/永劫藏宝阁 搜索 胡桃 2    # 翻到第 2 页
/永劫藏宝阁 搜索 胡桃 10   # 翻到第 10 页
```

### 3. 排序

支持按价格、收藏数升降序排序。

命令：

```text
/永劫藏宝阁 搜索 胡桃 价格升
/永劫藏宝阁 搜索 胡桃 价格降
/永劫藏宝阁 搜索 殷紫萍 收藏升
/永劫藏宝阁 搜索 殷紫萍 收藏降
```

默认按价格升序排列。

### 4. 公示期 / 在售期过滤

可以只搜索公示期或只在售期的商品。

命令：

```text
/永劫藏宝阁 搜索 胡桃 公示期
/永劫藏宝阁 搜索 胡桃 在售期
```

不指定时，公示期和在售期的商品都会显示。

### 5. 星格区间过滤

可以按星格区间筛选商品。星格字段为 `variation_id`，用 `-` 分隔多段星格（如 `1星格-2星格-3星格`）。

用 `N星格:区间` 指定某个星格的区间，多个星格用空格分隔：

```text
/永劫藏宝阁 搜索 胡桃 1星格:2000-3000
/永劫藏宝阁 搜索 胡桃 1星格:2000-3000 2星格:100-200
/永劫藏宝阁 搜索 胡桃 1星格:900-1000 3星格:1-5
```

未指定的星格（如上面第 3 条里的 2星格、4星格）不做限制。

与编号搜索不同，星格区间可能匹配多条商品，因此星格搜索会**遍历完整列表**再输出所有符合条件的商品，而不是命中第一条就停止。

指定星格区间时，插件会跨页扫描对应皮肤类型的全部商品（默认遍历全部页）并逐条按星格过滤，因此能返回所有符合星格区间的商品。可通过配置 `star_scan_pages` 限制扫描页数。

查询开始时，提示会显示预计查询全部页数所需的时间（如「⏳ 正在查询：胡桃，预计需要约 30 秒，请稍候……」，基于总页数和限流/并发配置估算）。

### 6. 价格区间过滤

可以按价格区间筛选商品。价格为人民币元，支持开放区间：

```text
/永劫藏宝阁 搜索 胡桃 价格:100-500
/永劫藏宝阁 搜索 胡桃 价格:5000-         # 只设下限，5000 元以上
/永劫藏宝阁 搜索 胡桃 价格:-1000         # 只设上限，1000 元以下
/永劫藏宝阁 搜索 胡桃 价格:500            # 精确匹配 500 元
```

与星格搜索一样，价格区间可能匹配多条商品，因此价格过滤也采用**跨页全扫**策略：遍历该皮肤类型的全部商品再按价格过滤（准确优先，慢但完整），而不是只过滤当前页。可通过配置 `star_scan_pages` 限制扫描页数。

### 7. 编号模糊搜索

支持按字母开头的编号（如 Y/S）进行模糊搜索。

```text
/永劫藏宝阁 搜索 Y007510
```

编号是唯一的，不会重复。因此编号搜索采用**命中即停**策略：扫描皮肤类型与页时，一旦找到匹配的编号就立即返回结果，不再遍历剩余部分，查询更快、请求更少。

### 8. 皮肤名 + 编号 组合搜索

可以同时指定皮肤名和编号，只在对应皮肤类型内模糊搜索该编号。

```text
/永劫藏宝阁 搜索 风之絮语 Y002481
/永劫藏宝阁 搜索 土御门胡桃 Y007
```

与单独的编号搜索（遍历全部皮肤类型）相比，组合搜索只在匹配的皮肤类型内扫描，速度更快、请求更少、更不易触发风控。

皮肤类型列表会分页抓取全部类型，即使某个皮肤变体不在第一页（不按出售时间排在前 30），组合搜索也会找到它所在的皮肤类型，从而不遗漏任何编号。

### 9. 查看商品详情

搜索结果里会直接给出藏宝阁详情链接。用户点击链接后，可以在官方页面查看完整外观、英雄、武器和账号资产。

### 10. 编号出现提醒

可以注册一个提醒，当某个皮肤的某个编号出现时，机器人主动发消息提醒你。

```text
/永劫藏宝阁 提醒 风之絮语 Y002481
/永劫藏宝阁 提醒列表
/永劫藏宝阁 取消提醒 Y002481
/永劫藏宝阁 取消提醒  # 取消我的全部提醒
提醒按用户区分：每个用户的提醒独立保存，提醒列表 只显示自己的提醒，取消提醒 只取消自己的提醒（同一群内互不影响）。
```

说明：

- 注册后插件会定时轮询该皮肤类型，编号出现时向你发送提醒（每个提醒只提醒一次）。
- 提醒检查是轻量的：只扫描该皮肤类型最前几页（默认 5 页，可配置 `reminder_scan_pages`）的最新上架商品，快速且不易触发风控。
- 提醒消息会**艾特发布提醒的用户**（不同平台支持度不同，个别平台可能退化为 @用户名 文本）。
- 提醒记录保存在插件的 `reminders.json` 文件中，重启后仍保留。
- 轮询间隔可在配置 `reminder_interval` 中调整（默认 60 秒）。

### 11. 手机短信风控验证（MOBILE_AUTH）

当藏宝阁触发手机短信风控（`MOBILE_AUTH`）时，插件会自动调用接口向账号绑定的手机号发送短信验证码，并提示你输入验证码：

```text
/永劫藏宝阁 验证码 123456
```

或直接发送验证码数字：

```text
/永劫藏宝阁 123456
```

验证通过后，插件会自动**重跑触发验证时的那次查询**，无需重新输入完整命令。

说明：

- 验证码由网易发送到账号**绑定的手机号**，插件无法代收，需要你手动输入。
- 发送验证码需要当前 Cookie 有效；若 Cookie 过期，会提示你先更新 Cookie。

### 12. 最低价查询

返回该皮肤当前最便宜的一件商品。

```text
/永劫藏宝阁 最低价 风之絮语
/永劫藏宝阁 最低价 胡桃
```

实现：按价格升序扫描该皮肤类型前几页（默认 5 页，可配置 `lowest_price_scan_pages`），在扫描到的在售商品中取全局最低价。由于接口按价格升序返回，最便宜的通常在第 1 页，因此请求量小、速度较快。

### 13. 收藏功能

可以收藏某个编号的商品，形成一份快照，之后可刷新查看最新价格。

```text
/永劫藏宝阁 收藏 Y002481       # 收藏某个编号的商品
/永劫藏宝阁 收藏列表            # 查看我的收藏
/永劫藏宝阁 刷新收藏            # 更新我的收藏的最新价格（逐条重查）
/永劫藏宝阁 取消收藏 Y002481    # 取消对应收藏
/永劫藏宝阁 取消收藏           # 取消我的全部收藏
```

说明：

- 收藏按用户区分，每个用户的收藏独立保存，'收藏列表' 只显示自己的，'取消收藏' 只取消自己的（同一群内互不影响）。
- 收藏保存 {编号、名称、变体、服务器、价格、链接} 快照，持久化到插件的 `favorites.json` 文件，重启后仍保留。
- `刷新收藏` 会逐条按编号重查并更新价格；已下架/查不到的商品会提示。

### 14. 诊断命令

检查插件的运行状态，便于排查 Cookie、接口与风控问题。

```text
/永劫藏宝阁 诊断
```

返回内容包括：

- 配置与依赖：基础地址、是否填写 Cookie、每页数量、并发/限流间隔、收藏数、提醒数、依赖 httpx 是否可用。
- 接口连通与风控状态：调用皮肤类型接口一次，报告返回的皮肤类型数量，以及是否触发风控（CAPTCHA/MOBILE_AUTH）。

### 15. 多条件组合过滤

以上过滤条件（公示期/在售期、价格区间、星格区间）与排序（价格/收藏升降序）可以在同一次搜索中**同时组合生效**。星格与价格区间采用跨页全扫，公示期/在售期与排序作用在全量结果上。

```text
/永劫藏宝阁 搜索 胡桃 公示期 价格:100-500 价格升
/永劫藏宝阁 搜索 胡桃 1星格:2000-3000 2星格:100-200 在售期 收藏降
/永劫藏宝阁 搜索 风之絮语 价格:5000- 价格降
```

### 16. 帮助命令

命令：

```text
/永劫藏宝阁 帮助
```

别名：

```text
/藏宝阁 帮助
/永劫cbg 帮助
```

## 搜索逻辑说明

本插件采用两步搜索策略，确保能返回完整数据：

1. **第一步**：调用 `get_aggregate_equip_type_list` 分页获取**全部**英雄皮肤类型列表（不限于第一页 30 个），并对关键词做模糊匹配，找到对应的皮肤类型（`equip_type`）。
2. **第二步**：用匹配到的 `equip_type` 调用 `recommend.py` 接口，获取该皮肤类型的全部在售商品（支持翻页，最多上百页）。

这种方式比直接调用 `keyword_query` 更准确，能返回该英雄/皮肤的全部商品数据，而不是只返回少量无关结果。

## 接口说明

当前使用以下藏宝阁接口：

| 接口 | 用途 |
| --- | --- |
| `get_aggregate_equip_type_list` | 获取皮肤类型聚合列表 |
| `recommend.py?act=recommd_by_role` | 获取指定皮肤类型的商品列表（支持翻页，编号/星格/价格过滤均基于此扫描） |
| `get_equip_desc` | 获取商品详情（卖家、洗练次数等） |

## 插件配置

插件配置文件为：

```text
_conf_schema.json
```

可配置项：

| 配置项 | 默认值 | 说明 |
| --- | --- | --- |
| `base_url` | `https://yjwujian.cbg.163.com` | 永劫无间藏宝阁站点地址 |
| `api_prefix` | `/cgi/api` | 默认 API 前缀 |
| `cookie` | 空 | 可选 Cookie，遇到登录态或风控限制时可填写 |
| `timeout_seconds` | `12` | 请求超时时间 |
| `max_results` | `5` | 搜索结果展示数量 |
| `debug_raw_error` | `false` | 开启后查询失败时返回更详细的接口错误信息 |
| `id_scan_pages` | `0` | 编号/组合搜索时每个皮肤类型扫描的页数（0 表示遍历全部页） |
| `list_order_by` | `attrs.client_show_id ASC` | 编号/组合搜索扫描列表时的排序参数。必须带排序才能拿到完整在售列表，否则接口只返回推荐子集、会漏掉部分编号 |
| `id_scan_interval` | `0.6` | 编号/星格搜索时每批请求间隔（秒），越大越不易触发风控 |
| `batch_pages` | `10` | 编号/星格搜索时每批查询的页数，每批查询完会休息 `batch_interval` 秒 |
| `batch_interval` | `5` | 每批之间等待秒数，越大越不易触发风控但越慢 |
| `request_concurrency` | `2` | 编号/星格搜索时并发抓取的页数，越大越快但越易触发风控 |
| `request_interval` | `1` | 全局限流，所有藏宝阁请求之间的最小间隔（秒），越大越不易触发风控但搜索越慢 |
| `page_size` | `50` | 每页请求数量，调大（如 100）可减少请求总数、更快且更不易触发风控；接口是否支持需实测 |
| `star_scan_pages` | `0` | 星格区间搜索时每个皮肤类型扫描的页数（0 表示遍历全部页） |
| `lowest_price_scan_pages` | `5` | `/最低价` 查询时按价格升序扫描的前几页，最便宜的通常在第 1 页 |
| `reminder_interval` | `60` | 编号提醒轮询间隔（秒），越大越不易触发风控 |
| `reminder_scan_pages` | `5` | 提醒检查时每个皮肤类型扫描的前几页（最新上架），越大越可能命中但请求更多 |

## 导入方式

将整个插件目录复制到 AstrBot 插件目录：

```text
data/plugins/astrbot_plugin_yjcbg
```

然后在 AstrBot WebUI 中重载插件。

依赖安装：

```bash
pip install -r requirements.txt
```

## 注意事项

藏宝阁没有公开稳定的第三方 API 文档。本插件根据当前网页前端使用的接口进行查询，因此存在以下限制：

- 网易调整接口后，可能需要修改 `api_prefix` 或代码。
- 部分数据可能需要登录态 Cookie。
- 高频请求可能触发风控。`CAPTCHA_AUTH`（安全验证）、`SESSION_TIMEOUT`（会话过期）会自动退避重试若干次，临时限流可自动恢复；若触发 `MOBILE_AUTH`（手机短信验证），插件会自动发送验证码到绑定手机，你在消息端输入验证码即可完成验证并自动重跑原查询。
- 商品价格、状态和上架时间以藏宝阁接口实时返回为准。
- 搜索结果中的详情链接会跳转到藏宝阁官方页面，完整资产信息以官方页面显示为准。

## 常见问题

### 搜索没有结果

可能原因：

- 关键词确实没有匹配的商品。
- 接口需要登录态。
- Cookie 失效。
- 藏宝阁接口路径发生变化。

可以换一个更具体的关键词重试，例如外观名称、武器名称或英雄名称。

### 查询失败

可以在插件配置中开启“显示详细错误”。开启后会显示更详细的错误信息，方便排查是网络、接口还是风控问题。